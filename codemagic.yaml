workflows:
  android-release:
    name: Android Release
    environment:
      flutter: stable
    scripts:
      - flutter pub get
      - flutter build appbundle
    artifacts:
      - build/**/outputs/**/*.aab

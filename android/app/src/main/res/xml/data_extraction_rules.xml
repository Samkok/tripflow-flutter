<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ data extraction rules. Disabling both cloud backup and
  device-to-device transfer keeps Hive caches and SharedPreferences
  (which may contain location data and the "remember me" email) from
  leaving the device sandbox. Authoritative state lives in Supabase,
  so disabling backup has no functional impact.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
